CREATE EXTENSION IF NOT EXISTS citext;

-- The canonical schema is defined in prisma/schema.prisma. Generate the SQL migration with:
-- npm.cmd --prefix backend run db:migrate
-- This placeholder exists so the repository records that migrations are required before deployment.
